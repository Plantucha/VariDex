--- a/varidex/io/loaders/clinvar.py
+++ b/varidex/io/loaders/clinvar.py
@@ -203,7 +203,8 @@ def load_clinvar_file(filepath, file_type=None, **kwargs):
     """
     try:
-        print(f"\n{'='*70}\nLOADING {file_type.upper()}: {filepath.name}\n{'='*70}")
+        from pathlib import Path
+        filepath = Path(filepath)
+        print(f"\n{'='*70}\nLOADING {file_type.upper() if file_type else 'CLINVAR'}: {filepath.name}\n{'='*70}")
